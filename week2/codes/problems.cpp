#include<iostream>
using namespace std;
int dist2(int a1)
int main(){
    int dist;
    cout<<"ENTER THE DISTANCE"<<endl;
    cin>>dist;  
    // distance= distance(a,b);   the variable is not declared anywhere and function distance is defined but at end of main and its prototype at start is also not given so it is unknown
    cout << "The fuel is: " <<dist2(dist);
    return 0;
}
// int distance(int a){  
int distance(int a){
    int a;
    a=a1*10;
return a;
}